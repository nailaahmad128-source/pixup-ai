<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Only present in debug/profile builds: required for Flutter's
         VM service (hot reload/DevTools). NOT included in the release
         manifest, so the shipped Play Store build stays fully offline
         with zero network permission. -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
